#include "macros/btlcmd.inc"

    .data

_000:
    CheckIgnoreWeather _006
    CompareVarToValue OPCODE_FLAG_SET, BTLVAR_FIELD_CONDITIONS, FIELD_CONDITION_RAINING, _028

_006:
    CompareMonDataToValue OPCODE_FLAG_SET, BTLSCR_ATTACKER, BATTLEMON_VOLATILE_STATUS, VOLATILE_CONDITION_MOVE_LOCKED, _033
    CheckItemHoldEffect CHECK_HAVE, BTLSCR_ATTACKER, HOLD_EFFECT_CHARGE_SKIP, _026
    UpdateVar OPCODE_SET, BTLVAR_SIDE_EFFECT_FLAGS_DIRECT, MOVE_SIDE_EFFECT_TO_ATTACKER|MOVE_SUBSCRIPT_PTR_VANISH_CHARGE_TURN
    UpdateVar OPCODE_SET, BTLVAR_SIDE_EFFECT_FLAGS_INDIRECT, MOVE_SIDE_EFFECT_TO_ATTACKER|MOVE_SUBSCRIPT_PTR_SP_ATTACK_UP_1_STAGE
    UpdateVar OPCODE_FLAG_ON, BTLVAR_BATTLE_CTX_STATUS, SYSCTL_FIRST_OF_MULTI_TURN|SYSCTL_CHECK_LOOP_ONLY_ONCE|SYSCTL_SKIP_ATTACK_MESSAGE
    End 

_026:
    Call BATTLE_SUBSCRIPT_POWER_HERB_METEOR_BEAM
    CompareVarToValue OPCODE_EQU, BTLVAR_DEFENDER, BATTLER_NONE, _035
    GoTo _033

_028:
    PrintAttackMessage
    Wait 
    WaitButtonABTime 30
    BufferMessage pl_msg_00000368_00214, TAG_NICKNAME, BTLSCR_ATTACKER
    Call BATTLE_SUBSCRIPT_SP_ATK_UP_RAIN_SKIP /* Need a separate subscript here so the Power Herb isnt animated and removed. */
    CompareVarToValue OPCODE_EQU, BTLVAR_DEFENDER, BATTLER_NONE, _035

_033:
    CalcCrit 
    CalcDamage 

_035:
    Call BATTLE_SUBSCRIPT_CHARGE_MOVE_CLEANUP
    End 