#ifndef POKEPLATINUM_MACROS_H
#define POKEPLATINUM_MACROS_H

#define MIN(a,b) (((a)<(b))?(a):(b))
#define MAX(a,b) (((a)>(b))?(a):(b))

#define NELEMS(x) (sizeof(x) / sizeof((x)[0]))

#endif // POKEPLATINUM_MACROS_H